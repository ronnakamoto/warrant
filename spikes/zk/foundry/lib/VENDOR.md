Vendored for the Foundry spike so solc is not asked to read `spikes/node_modules`.

- `lean-imt/` — `@zk-kit/lean-imt.sol@2.0.1` (MIT). `InternalLeanIMT.sol` + `Constants.sol`.
- `poseidon-solidity/` — `poseidon-solidity@0.0.5` (MIT). `PoseidonT3.sol` + `PoseidonT5.sol`.

Do not edit. Re-copy from npm if either package moves.
